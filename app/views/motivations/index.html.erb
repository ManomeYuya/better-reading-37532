<main class="main">
    <% if user_signed_in? %>
      <div class="greeting">
      <%= link_to "#{current_user.nickname}さん、ようこそ！" %>
      <p>読みたい本を投稿してみよう！</p>
      </div>
    <% end %>
  <div class="inner">
      <div class="card__wrapper">
      <% @motivations.each do |motivation| %>
       <%= link_to motivation_path(motivation.id) do %>
          <div class='content_post'>
          <%= image_tag motivation.image.variant(resize: '500x500'), class: "item-img" if motivation.image.attached?  %>
          <span class="name">
      </div>
      <% end %>
      <div class="detail">
        <div class="title">
          <%= "投稿者: #{motivation.user.nickname}" %>
        </div>
        <div class="title">
          <%= "タイトル: #{motivation.title}" %>
        </div>
        <div class="purchase_date">
          <%= "購入日: #{motivation.purchase_date}" %>
        </div>
        <div class="category">
          <%= "カテゴリー: #{motivation.category.name}" %>
        </div>
        </div>
        
      <%# <% if motivation.favorited?(current_user) %>  
    <%# 一致するレコードが存在する＝すでにいいね済みの場合はdestroyアクションへ %>
      <%# <%= link_to motivation_favorites_path(motivation.id), method: :delete do %> 
        <span style="color:red;">❤︎</span>
         <%# <% end %> 
    <%# 一致するレコードが存在しない＝まだいいねしていない場合はcreateアクションへ %>
      <%# <%= link_to motivation_favorites_path(motivation.id), method: :post do %> 
        <span>❤︎</span>
        <%# <% end %> 
     <%# <% end %>  
      </span>
      
    
    <% end %>
    </div>
  </div>
  <%= form_with(url: search_motivations_path, local: true, method: :get, class: "search-form") do |form| %>
  <%= form.text_field :keyword, placeholder: "タイトルを検索", class: "search-input" %>
  <%= form.submit "検索", class: "search-btn" %>
<% end %>
</main>
<div class="footer">
 <div class="copyright">
 </div>
</div>